Reproducibility

You should eventually be able to say:

Give me the same code, configuration, dataset, dependencies and seed, and I can reproduce the experiment.

Track:

Git commit
Dataset version
Python version
Package versions
Configuration
Random seed
Model parameters
Training environment

This is where concepts like experiment lineage become important.